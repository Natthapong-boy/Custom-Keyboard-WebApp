# Custom Keyboard UI (scaffold)

This is a UI-only scaffold for a custom keyboard shop using Vite + React + Tailwind.

Run locally after installing dependencies:

```bash
npm install
npm run dev
```

I'll implement interactive features next or install packages if you approve.
